const fs = require('fs');
const path = require('path');

const DEFAULT_DIR = path.resolve(__dirname, '../public');
const ROUTES_ROOT_DIR = path.resolve(__dirname, '../public/routes/');
const ROUTES_DIRS = ['routefiles', 'routeimages', 'pointimages'];

const mkRouteDirectories = (routeId) => {};

//iside methods

const _mkDirectory = (path, dirName) => {};
